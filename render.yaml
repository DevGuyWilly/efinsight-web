# Render Blueprint for the EFinSight web app (static site).
#
# The browser only ever talks to this site. In dev Vite proxies the API; on Render these rewrite rules do the same,
# so the backend needs no CORS setup and the TrueLayer redirect lands on this origin.
#
# The API is https://efinsight-api.onrender.com. If it moves, update the three destinations below.
services:
  - type: web
    name: efinsight-web
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: "22"
    routes:
      # Order matters: specific proxy rules first, single-page-app fallback last.
      - type: rewrite
        source: /api/*
        destination: https://efinsight-api.onrender.com/api/*
      - type: rewrite
        source: /callback
        destination: https://efinsight-api.onrender.com/callback
      - type: rewrite
        source: /auth/connect-bank
        destination: https://efinsight-api.onrender.com/auth/connect-bank
      # /auth/success and /auth/error are React routes, so they fall through to the app.
      - type: rewrite
        source: /*
        destination: /index.html
